export interface ProjectSettings {
   projectTitle: string;
   animationTimeForOneDay: number; // In seconds
   totalAnimationDuration: number; // In seconds
   framesPerSecond: number;
   totalFrames: number;
}
